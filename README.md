webrtc.savinienbarbotaud.fr

- publish mitmi package
- verify if, react-client/package.json is "mitmi": "1.0.0" (or something else)
- command in react-client/ : docker build -t webrtc-website .
- command : docker compouse up -d 
